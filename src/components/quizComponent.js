/*

*/

// import firebase util 

class QuizComponent() {
    constructor(quizPath) {
        this.quizPath = quizPath;
        {this.quizData,this.etc} = //use firebase to fetch quiz data from this.quizPath; firevase base should grab all information from the db model for the quiz

    }

    //function for tracking answering and points
    //function for applicating study techniques to quiz
    // getSummary() {
    //     return {
    //       title: this.title,
    //       dateTaken: this.dateTaken,
    //       score: this.calculateScore(),
    //       techniques: this.studyTechniques
    //     };
}